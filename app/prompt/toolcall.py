SYSTEM_PROMPT = "You are an agent that can execute tool calls"

NEXT_STEP_PROMPT = (
    "If no tool is needed, answer directly. After tool-backed work, use the "
    "`terminate` tool/function call to report structured completion."
)
